import React from 'react'; //importa em todo arquivo que utiliza html dentro dele.
import ReactDOM from 'react-dom'; //se comunica com a arvore de elementos ele varia para cada ambiente(mobile, tv, web)
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

//jsx (JavaScript + xml)